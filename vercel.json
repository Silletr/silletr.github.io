{
  "builds": [
    {
      "src": "Gemfile",
      "use": "@vercel/ruby"
    },
    {
      "src": "_config.yml",
      "use": "@vercel/static-build",
      "config": { "distDir": "_site" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/_site/$1" }
  ]
}

